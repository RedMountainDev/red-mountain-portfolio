import {IExperienceCategory} from '../../../../interfaces/IExperienceCategory';
import {css3} from './css3';
import {bootstrap} from './bootstrap';
import {sass} from './sass';

export const cssCat: IExperienceCategory = {
  title: 'CSS',
  areas: [
    css3,
    bootstrap,
    sass
  ]
};
